
// Lógica de Assinatura Paisagem
const canvas = document.getElementById('sigCanvas');
const ctx = canvas.getContext('2d');
// Garantir dimensão paisagem
canvas.width = 500;
canvas.height = 200;

function carregarEquipamentos() {
    const clienteId = document.getElementById('select-cliente').value;
    const selectEq = document.getElementById('select-equipamento');
    // Lógica para filtrar equipamentos vinculados ao cliente selecionado
    // ...
}

function iniciarOS() {
    // Lógica de cronômetro e fluxo de salvamento ou edição
}
